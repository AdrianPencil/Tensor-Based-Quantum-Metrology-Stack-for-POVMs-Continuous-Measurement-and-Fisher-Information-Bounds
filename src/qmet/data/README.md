qmet data folder

This folder contains small example datasets used for demos, docs, and quick checks.

Files:
- example_psd.csv: a tiny frequency/PSD table (units: Hz and (unit)^2/Hz)
- example_allan.csv: a tiny tau/Allan-deviation table (units: s and unit)

These are intentionally small.
